"use client";
import { useEffect } from 'react';
import { flushSync } from 'react-dom';
type Context={registerTool:(tool:{name:string;description:string;inputSchema:object;annotations:object;execute:(input:unknown)=>unknown},options:{signal:AbortSignal})=>void|Promise<void>};
export function useJournalTools(onOpen:(date:string)=>void){useEffect(()=>{const context=(document as Document & {modelContext?:Context}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();Promise.resolve(context.registerTool({name:'open_day_statistics',description:'Открывает статистику выбранного дня в календаре торгового журнала. Данные демонстрационные.',inputSchema:{type:'object',properties:{date:{type:'string',description:'YYYY-MM-DD'}},required:['date'],additionalProperties:false},annotations:{readOnlyHint:false},execute(input){const date=(input as {date?:unknown})?.date;if(typeof date!=='string'||!/^\d{4}-\d{2}-\d{2}$/.test(date)||Number.isNaN(Date.parse(date))||new Date(date).toISOString().slice(0,10)!==date)throw new Error('Укажите действительную дату YYYY-MM-DD');flushSync(()=>onOpen(date));return {openedDate:date,source:'demo'};}},{signal:lifecycle.signal})).catch(()=>{});return ()=>lifecycle.abort();},[onOpen])}
